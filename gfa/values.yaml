# Default values for gfa.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

defaultSpringBootPort: 8085
microservicesPort: 8080
db:
  ip: 192.168.176.147
  port: 1521
  url: jdbc:oracle:thin:@db-service:1521/XEPDB1

global:
  redis:
    password: redis_psw

redis:
  architecture: standalone
  replica.replicaCount: 1